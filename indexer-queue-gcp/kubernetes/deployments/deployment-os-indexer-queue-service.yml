apiVersion: v1
kind: Service
metadata:
  name: os-indexer-queue-service
  labels:
    app: os-indexer-queue-service
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: os-indexer-queue-service
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: os-indexer-queue-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: os-indexer-queue-service
        version: v1
    spec:
      containers:
        - name: os-indexer-queue-app
          envFrom:
            - configMapRef:
                name: os-service-config
          image: gcr.io/opendes/os-indexer-queue-app
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
