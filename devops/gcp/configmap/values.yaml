# Default values for indexer-queue-configmap.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

data:
  # common
  log_level: "INFO"
  osdu_entitlements_url: "http://entitlements/api/entitlements/v2"
  partition_api: "http://partition/api/partition/v1/"
  spring_profiles_active: "gcp"
  # gcp
  cloud_task_target_host: ""
  google_audiences: ""
  google_cloud_project: ""
  google_cloud_project_region: ""
  indexer_queue_identifier: ""
  # anthos
  indexer_host: "http://indexer"
conf:
  app_name: "indexer-queue"
  configmap: "indexer-queue-config"
  on_prem_enabled: false
