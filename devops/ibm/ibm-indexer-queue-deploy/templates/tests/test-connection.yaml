apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ibm-indexer-queue-deploy.fullname" . }}-test-connection"
  labels:
    {{- include "ibm-indexer-queue-deploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ibm-indexer-queue-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
